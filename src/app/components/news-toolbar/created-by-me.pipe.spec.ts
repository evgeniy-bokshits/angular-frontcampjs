import { CreatedByMePipe } from './created-by-me.pipe';

describe('CreatedByMePipe', () => {
  it('create an instance', () => {
    const pipe = new CreatedByMePipe();
    expect(pipe).toBeTruthy();
  });
});
